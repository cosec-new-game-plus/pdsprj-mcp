# Documentation

- [`format.md`](format.md) — `.pdsprj` binary format reference. What
  the parser reads from `ROOT.DSN` and `ROOT.CDB`, with byte-level
  layouts and parsing heuristics.
- [`limitations.md`](limitations.md) — why this server reads and
  modifies but does not generate schematics. Failure modes encountered
  while reverse-engineering the format.
